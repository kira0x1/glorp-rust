apiVersion: v1
kind: Service
metadata:
  name: glorp-service
  labels:
    app: glorp
spec:
  ports:
  - name: http
    port: 3000
    nodePort: 30000
    protocol: TCP
  type: NodePort
  selector:
    app: glorp